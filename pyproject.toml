[project]
name = "flask-socketio-chat"
version = "1.0"
dependencies = [
    "flask",
    "flask-socketio",
    "flask-wtf",
    "eventlet",
    "gunicorn"
]

[project.optional-dependencies]
dev = [
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
